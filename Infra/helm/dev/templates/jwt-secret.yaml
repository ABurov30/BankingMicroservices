{{- if and .Values.jwt.privateKey .Values.jwt.publicKey }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.jwt.secretName }}
type: Opaque
stringData:
  private.pem: |-
{{ .Values.jwt.privateKey | indent 4 }}
  public.pem: |-
{{ .Values.jwt.publicKey | indent 4 }}
{{- end }}
